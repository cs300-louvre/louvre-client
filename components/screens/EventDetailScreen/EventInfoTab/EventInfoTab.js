import { View, Text, ScrollView } from "react-native";


export default function EventInfoTab() {
    return (
        <ScrollView
            style={{ paddingTop: 60 }}
        >
            <Text style={{
                color: "#FFFFFF",
            }}>Event Info</Text>
        </ScrollView>
    );
}