import { View, Text, ScrollView } from "react-native";

export default function EventReviewsTab() {
    return (
        <ScrollView
            style={{ paddingTop: 60 }}
        >
            <Text style={{
                color: "#FFFFFF",
            }}>Event Reviews</Text>
        </ScrollView>
    );
}