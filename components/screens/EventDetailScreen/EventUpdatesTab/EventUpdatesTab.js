import { View, Text, ScrollView } from "react-native";

export default function EventUpdatesTab() {
    return (
        <ScrollView
            style={{ paddingTop: 60 }}
        >
            <Text style={{
                color: "#FFFFFF",
            }}>Event Updates</Text>
        </ScrollView>
    );
}